'use strict';

/**
 * generic-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generic-question-answer.generic-question-answer');
