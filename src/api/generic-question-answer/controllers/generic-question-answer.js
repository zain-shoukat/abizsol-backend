'use strict';

/**
 * generic-question-answer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::generic-question-answer.generic-question-answer');
