'use strict';

/**
 * client-section-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::client-section-card.client-section-card');
