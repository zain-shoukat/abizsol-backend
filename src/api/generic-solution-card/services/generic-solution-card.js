'use strict';

/**
 * generic-solution-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generic-solution-card.generic-solution-card');
