'use strict';

/**
 * generic-solution-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::generic-solution-card.generic-solution-card');
