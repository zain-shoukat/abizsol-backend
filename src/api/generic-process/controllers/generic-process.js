'use strict';

/**
 * generic-process controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::generic-process.generic-process');
