'use strict';

/**
 * generic-process-card controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::generic-process-card.generic-process-card');
