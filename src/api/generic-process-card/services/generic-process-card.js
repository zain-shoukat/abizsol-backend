'use strict';

/**
 * generic-process-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generic-process-card.generic-process-card');
